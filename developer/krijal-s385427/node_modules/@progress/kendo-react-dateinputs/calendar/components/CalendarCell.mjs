/**
 * @license
 *-------------------------------------------------------------------------------------------
 * Copyright © 2026 Progress Software Corporation. All rights reserved.
 * Licensed under commercial license. See LICENSE.md in the package root for more information
 *-------------------------------------------------------------------------------------------
 */
import * as o from "react";
import { classNames as S, uCalendar as C } from "@progress/kendo-react-common";
const A = (a, n) => {
  const s = Object.getOwnPropertyNames(a), r = Object.getOwnPropertyNames(n);
  if (s.length !== r.length)
    return !1;
  for (let t = 0; t < s.length; t++) {
    const l = s[t];
    if (a[l] !== n[l])
      return !1;
  }
  return !0;
}, I = o.memo(
  (a) => {
    const {
      className: n,
      formattedValue: s,
      isWeekend: r,
      isFocused: t,
      isInRange: l,
      isSelected: u,
      isRangeStart: i,
      isRangeMid: N,
      isRangeEnd: c,
      isRangeSplitStart: y,
      isRangeSplitEnd: k,
      isToday: w,
      isDisabled: O,
      view: D,
      value: d,
      isOtherMonth: e,
      showOtherMonthDays: m,
      allowReverse: g,
      unstyled: p,
      activeRangeEnd: f,
      children: b,
      onClick: v,
      onMouseEnter: E,
      onMouseLeave: h,
      ...L
    } = a, P = (x) => {
      v && v(d, x);
    }, T = () => {
      E && E(d);
    }, V = () => {
      h && h(d);
    }, j = f === "end" && c, q = f === "start" && i, R = p && p.uCalendar, M = S(
      C.td({
        c: R,
        rangeStart: !e && !g && i,
        rangeEnd: !e && !g && c,
        rangeMid: !e && N,
        rangeSplitEnd: !e && k,
        rangeSplitStart: !e && y,
        active: q || j,
        focused: t,
        selected: !e && (u || i || c),
        today: !e && w,
        weekend: r,
        disabled: O,
        isOtherMonth: e,
        isEmpty: !m && e
      }),
      n
    );
    return !m && e ? /* @__PURE__ */ o.createElement("td", { role: "gridcell", className: M }) : /* @__PURE__ */ o.createElement(
      "td",
      {
        ...L,
        className: M,
        onClick: P,
        onMouseEnter: T,
        onMouseLeave: V
      },
      /* @__PURE__ */ o.createElement("span", { className: S(C.link({ c: R })) }, b)
    );
  },
  (a, n) => {
    if (!a || !n)
      return !1;
    const { value: s, ...r } = a, { value: t, ...l } = n;
    return (!(s && t) || s.getTime() === t.getTime()) && A(r, l);
  }
);
export {
  I as CalendarCell
};
