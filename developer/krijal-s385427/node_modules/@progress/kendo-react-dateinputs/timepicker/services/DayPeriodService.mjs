/**
 * @license
 *-------------------------------------------------------------------------------------------
 * Copyright © 2026 Progress Software Corporation. All rights reserved.
 * Licensed under commercial license. See LICENSE.md in the package root for more information
 *-------------------------------------------------------------------------------------------
 */
import { cloneDate as l } from "@progress/kendo-date-math";
const u = (s, t) => {
  const n = l(s);
  return n.setHours(t), n;
}, h = (s) => s !== null && s < 12, c = (s) => s !== null && s >= 12, g = (s, t, n) => t == null && n == null || s >= t && s <= n, d = (s, t, n) => t == null && n == null || s >= t || s <= n;
class H {
  constructor(t) {
    this.intl = t, this.min = null, this.max = null, this.part = null;
  }
  /**
   * @hidden
   */
  apply(t, n) {
    const i = t.getHours(), e = h(i), r = h(n.getHours());
    if (e && r || !e && !r)
      return t;
    const [o, a = 24] = this.normalizedRange(), m = i + (r ? -12 : 12);
    return u(t, Math.min(Math.max(o, m), a != null ? a : 24));
  }
  /**
   * @hidden
   */
  configure(t) {
    const { min: n = this.min, max: i = this.max, part: e = this.part } = t;
    this.min = n, this.max = i, this.part = e;
  }
  /**
   * @hidden
   */
  data(t) {
    const n = this.part && this.part.names;
    if (!n || !this.min)
      return [];
    const i = [], [e, r] = this.normalizedRange(), o = this.intl.dateFormatNames(n);
    return h(e) && i.push({ text: o.am, value: u(this.min, e) }), c(r) && i.push({ text: o.pm, value: u(this.min, Math.max(12, r)) }), this.min.getHours() !== e ? i.reverse() : i;
  }
  /**
   * @hidden
   */
  isRangeChanged(t, n) {
    return !1;
  }
  /**
   * @hidden
   */
  limitRange(t, n, i) {
    return [t, n];
  }
  /**
   * @hidden
   */
  total() {
    const [t, n] = this.normalizedRange();
    return t == null && n == null ? 2 : t > 11 || n < 12 ? 1 : 2;
  }
  /**
   * @hidden
   */
  selectedIndex(t) {
    if (!this.valueInList(t) || !this.min)
      return -1;
    const n = Math.floor(t.getHours() / 12);
    return this.min.getHours() === this.normalizedRange()[0] ? n : n === 0 ? 1 : 0;
  }
  /**
   * @hidden
   */
  valueInList(t) {
    return !this.min || !this.max ? !1 : (this.min.getHours() !== this.normalizedRange()[0] ? d : g)(t.getHours(), this.min.getHours(), this.max.getHours());
  }
  normalizedRange() {
    const t = this.min.getHours(), n = this.max.getHours();
    return [Math.min(t, n), Math.max(t, n)];
  }
}
export {
  H as DayPeriodService
};
