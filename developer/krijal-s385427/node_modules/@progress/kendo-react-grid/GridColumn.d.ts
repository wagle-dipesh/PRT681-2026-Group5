/**
 * @license
 *-------------------------------------------------------------------------------------------
 * Copyright © 2026 Progress Software Corporation. All rights reserved.
 * Licensed under commercial license. See LICENSE.md in the package root for more information
 *-------------------------------------------------------------------------------------------
 */
import { GridColumnProps } from './interfaces/GridColumnProps';
import { GridRowSpannableSettings } from './interfaces/GridRowSpannableSettings';
import * as React from 'react';
/**
 * Represents the GridColumn component.
 *
 * @returns null
 */
export declare const GridColumn: React.FunctionComponent<GridColumnProps>;
/**
 * @hidden
 */
export declare const gridDefaultProps: {
    filterable: boolean;
    editable: boolean;
    sortable: boolean;
    resizable: boolean;
    reorderable: boolean;
    groupable: boolean;
};
/**
 * @hidden
 */
export interface ExtendedColumnProps extends GridColumnProps {
    declarationIndex: number;
    parentIndex: number;
    rowSpan: number;
    depth: number;
    colSpan: number;
    kFirst?: boolean;
    index: number;
    headerColSpan: number;
    children: ExtendedColumnProps[];
    left: number;
    right: number;
    rightBorder: boolean;
    ariaColumnIndex: number;
    isAccessible: boolean;
    /** @hidden _internal usage only */
    _type?: 'edit' | 'expand';
    /** @hidden _internal usage only */
    isGroupColumn?: boolean;
    rowSpannable?: GridRowSpannableSettings & {
        enabled: boolean;
    };
    defaultCell?: any;
    defaultHeaderCell?: any;
    /**
     * @hidden _internal usage only.
     * The real field this synthesized group column represents (e.g. `'Category.CategoryName'`).
     * The column's own `field` is always the literal `'value'` placeholder used for cell
     * rendering (see `cells/groupcell/utils.tsx`), so `groupField` is the only place the
     * actual grouped field for this level is exposed — used by `HeaderRow.tsx` to report the
     * correct field to `groupColumn.columnMenu`/`columnMenuWrapperProps`, so a reused
     * `GridColumnMenuGroup` can correctly detect "already grouped" and offer Ungroup instead
     * of adding a bogus new group level.
     */
    groupField?: string;
    /** @hidden _internal usage only. Forces no column menu (e.g. suppressing groupColumn.columnMenu in `singleColumn` mode), overriding any grid-level default. */
    disableColumnMenu?: boolean;
}
