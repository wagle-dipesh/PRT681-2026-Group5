/**
 * @license
 *-------------------------------------------------------------------------------------------
 * Copyright © 2026 Progress Software Corporation. All rights reserved.
 * Licensed under commercial license. See LICENSE.md in the package root for more information
 *-------------------------------------------------------------------------------------------
 */
import { base as o, forms as t, orientationMap as l, sizeMap as m } from "./json-classes.mjs";
const n = `${o.prefix}-${t.form}`, $ = `${o.prefix}-${t.form}-${t.field}`, a = {
  form: {
    main: n,
    size: {
      prefix: `${n}-`,
      small: `${n}-${m.small}`,
      medium: `${n}-${m.medium}`,
      large: `${n}-${m.large}`
    },
    orientation: {
      horizontal: `${n}-${l.horizontal}`,
      vertical: `${n}-${l.vertical}`
    }
  },
  formLayout: {
    main: `${o.prefix}-${t.form}-layout ${o.prefix}-d-grid`
  },
  fieldset: {
    main: `${o.prefix}-${t.form}-${t.fieldset}`
  },
  legend: {
    main: `${o.prefix}-${t.form}-${t.legend}`
  },
  field: {
    main: $,
    isRtl: `${o.prefix}-${o.rtl}`
  },
  separator: {
    main: `${o.prefix}-${t.form}-${t.separator}`
  }
}, d = {
  form: (r) => {
    const { size: e, orientation: i, c: f = a } = r, s = f.form;
    return {
      [s.main]: !0,
      [s.size[e]]: s.size[e],
      [s.size.prefix || ""]: e && !s.size[e],
      [s.orientation[i]]: s.orientation[i]
    };
  },
  formLayout: (r) => {
    const { c: e = a } = r;
    return {
      [e.formLayout.main]: !0
    };
  },
  fieldset: (r) => {
    const { c: e = a } = r;
    return {
      [e.fieldset.main]: !0
    };
  },
  legend: (r) => {
    const { c: e = a } = r;
    return {
      [e.legend.main]: !0
    };
  },
  field: (r) => {
    const { isRtl: e, c: i = a } = r, f = i.field;
    return {
      [f.main]: !0,
      [f.isRtl]: e
    };
  },
  separator: (r) => {
    const { c: e = a } = r;
    return {
      [e.separator.main]: !0
    };
  }
};
export {
  d as uForm
};
