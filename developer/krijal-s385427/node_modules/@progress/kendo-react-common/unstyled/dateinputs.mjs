/**
 * @license
 *-------------------------------------------------------------------------------------------
 * Copyright © 2026 Progress Software Corporation. All rights reserved.
 * Licensed under commercial license. See LICENSE.md in the package root for more information
 *-------------------------------------------------------------------------------------------
 */
import { getClassByName as d } from "./interfaces/common.mjs";
import { calendarPrefix as c, dateInputs as n, base as e, elements as x, states as s, orientationMap as B, containers as o, cssUtils as I, actions as y, directionMap as P, sizeMap as b, inputs as W, inputPrefix as p, roundedMap as z, fillModeMap as M, components as v } from "./json-classes.mjs";
const f = {
  footer: {
    main: `${e.prefix}-${n.time}-${x.footer}`,
    actions: `${e.prefix}-${o.actions}`,
    stretched: `${e.prefix}-${o.actions}-${s.stretched}`,
    horizontal: `${e.prefix}-${o.actions}-${B.horizontal}`
  },
  cancel: `${e.prefix}-${n.time}-${y.cancel}`,
  accept: `${e.prefix}-${n.time}-${y.accept}`,
  part: {
    main: `${e.prefix}-${n.time}-${o.part}`,
    disabled: `${e.prefix}-${s.disabled}`
  },
  header: `${e.prefix}-${n.time}-${x.header}`,
  now: `${e.prefix}-${n.time}-${n.now}`,
  listContainer: `${e.prefix}-${n.time}-${o.list}-${o.container}`,
  highlight: `${e.prefix}-${n.time}-${s.highlight}`,
  listWrapper: {
    main: `${e.prefix}-${n.time}-${o.list}-${o.wrapper}`,
    focused: `${e.prefix}-${s.focus}`
  },
  list: `${e.prefix}-${n.time}-${o.list}`,
  container: {
    main: `${e.prefix}-${n.time}-${o.container}`,
    scrollable: `${e.prefix}-${s.scrollable}`,
    content: `${e.prefix}-${o.content}`
  },
  containerSelector: `${e.prefix}-${n.time}-${o.container}`,
  separator: `${e.prefix}-${n.time}-${o.separator}`,
  ul: `${e.prefix}-${e.reset}`,
  li: `${e.prefix}-${o.item}`,
  title: `${e.prefix}-${x.title}`,
  scrollablePlaceholder: `${e.prefix}-${s.scrollable}-${o.placeholder}`
}, R = {
  footer: (t) => {
    const { c: a = f } = t, r = a.footer;
    return {
      [r.main]: !0,
      [r.actions]: !0,
      [r.stretched]: !0,
      [r.horizontal]: !0
    };
  },
  cancel: d(f, "cancel"),
  accept: d(f, "accept"),
  part: (t) => {
    const { c: a = f, mobileMode: r, disabled: i } = t, l = a.part;
    return {
      [l.main]: r,
      [l.disabled]: i
    };
  },
  header: d(f, "header"),
  now: d(f, "now"),
  listContainer: d(f, "listContainer"),
  highlight: d(f, "highlight"),
  listWrapper: (t) => {
    const { c: a = f, focused: r } = t, i = a.listWrapper;
    return {
      [i.main]: !0,
      [i.focused]: r
    };
  },
  list: d(f, "list"),
  container: (t) => {
    const { c: a = f, content: r, scrollable: i } = t, l = a.container;
    return {
      [l.main]: !0,
      [l.content]: r,
      [l.scrollable]: i
    };
  },
  containerSelector: d(f, "containerSelector"),
  separator: d(f, "separator"),
  ul: d(f, "ul"),
  li: d(f, "li"),
  title: d(f, "title"),
  scrollablePlaceholder: d(f, "scrollablePlaceholder")
}, q = {
  wrapper: {
    main: `${e.prefix}-${v.timepicker}`,
    input: `${p}`,
    size: {
      small: `${p}-${b.small}`,
      medium: `${p}-${b.medium}`,
      large: `${p}-${b.large}`
    },
    fillMode: {
      solid: `${p}-${M.solid}`,
      outline: `${p}-${M.outline}`,
      flat: `${p}-${M.flat}`
    },
    rounded: {
      small: `${e.prefix}-${e.rounded}-${z.small}`,
      medium: `${e.prefix}-${e.rounded}-${z.medium}`,
      large: `${e.prefix}-${e.rounded}-${z.large}`
    },
    disabled: `${e.prefix}-${s.disabled}`,
    required: `${e.prefix}-${s.required}`,
    invalid: `${e.prefix}-${s.invalid}`
  },
  inputButton: `${p}-${x.button}`,
  popup: {
    main: `${e.prefix}-${v.timepicker}-${o.popup}`,
    container: `${e.prefix}-${o.list}-${o.container}`
  },
  timeSelector: {
    main: `${e.prefix}-${n.timeselector}`,
    size: {
      medium: `${e.prefix}-${n.timeselector}-${b.medium}`,
      large: `${e.prefix}-${n.timeselector}-${b.large}`
    },
    disabled: `${e.prefix}-${s.disabled}`,
    reset: `${e.prefix}-${e.reset}`
  }
}, U = {
  wrapper: (t) => {
    const { c: a = q, size: r, fillMode: i, rounded: l, disabled: h, required: g, invalid: k } = t, $ = a.wrapper;
    return {
      [$.main]: !0,
      [$.input]: !0,
      [$.size[r]]: $.size[r],
      [`${p}-${r}`]: r && !$.size[r],
      [$.fillMode[i]]: $.fillMode[i],
      [$.rounded[l]]: $.rounded[l],
      [`${e.prefix}-${e.rounded}-${l}`]: l && !$.rounded[l],
      [$.disabled]: h,
      [$.invalid]: k,
      [$.required]: g
    };
  },
  inputButton: d(q, "inputButton"),
  popup: (t) => {
    const { c: a = q } = t, r = a.popup;
    return {
      [r.main]: !0,
      [r.container]: !0
    };
  },
  timeSelector: (t) => {
    const { c: a = q, disabled: r, mobileMode: i } = t, l = a.timeSelector;
    return {
      [l.main]: !0,
      [l.size.large]: i,
      [l.disabled]: r
    };
  }
}, u = {
  wrapper: {
    main: `${c}`,
    infinite: `${c}-${e.infinite}`,
    disabled: `${e.prefix}-${s.disabled}`,
    weekNumber: `${e.prefix}-${n.week}-${n.number}`,
    size: {
      medium: `${c}-${b.medium}`,
      large: `${c}-${b.large}`
    }
  },
  view: {
    main: `${c}-${o.view}`,
    vertical: `${e.prefix}-${I.vstack}`,
    month: `${c}-${n.month}${o.view}`,
    year: `${c}-${n.year}${o.view}`,
    decade: `${c}-${n.decade}${o.view}`,
    century: `${c}-${n.century}${o.view}`
  },
  navigation: `${c}-${n.navigation}`,
  navigationHighlight: `${c}-${n.navigation}-${s.highlight}`,
  table: {
    main: `${c}-${x.table}`,
    weekdays: `${c}-${n.weekdays}`
  },
  thead: `${c}-${x.thead}`,
  tr: `${c}-${x.tr}`,
  th: `${c}-${x.th}`,
  caption: `${c}-${x.caption}`,
  tbody: `${c}-${x.tbody}`,
  ul: `${e.prefix}-${e.reset}`,
  li: "",
  td: {
    main: `${c}-${x.td}`,
    rangeStart: `${e.prefix}-${n.range}-${P.start}`,
    rangeEnd: `${e.prefix}-${n.range}-${P.end}`,
    rangeMid: `${e.prefix}-${n.range}-${P.mid}`,
    rangeSplitEnd: `${e.prefix}-${n.range}-${y.split}-${P.end}`,
    rangeSplitStart: `${e.prefix}-${n.range}-${y.split}-${P.start}`,
    active: `${e.prefix}-${s.active}`,
    focused: `${e.prefix}-${e.state}-${s.pending}-${s.focus}`,
    selected: `${e.prefix}-${s.selected}`,
    today: `${e.prefix}-${n.today}`,
    weekend: `${e.prefix}-${n.weekend}`,
    disabled: `${e.prefix}-${s.disabled}`,
    isOtherMonth: `${e.prefix}-${n.other}-${n.month}`,
    isEmpty: `${e.prefix}-${s.empty}`,
    isWeek: `${e.prefix}-${s.alt}`
  },
  title: `${c}-${x.title}`,
  header: {
    main: `${c}-${x.header}`,
    vertical: `${e.prefix}-${I.hstack}`
  },
  spacer: `${e.prefix}-${o.spacer}`,
  nav: `${c}-${o.nav}`,
  today: {
    main: `${c}-${o.nav}-${n.today}`,
    disabled: `${e.prefix}-${s.disabled}`
  },
  scrollable: {
    main: `${e.prefix}-${s.scrollable}`,
    content: `${e.prefix}-${o.content}`,
    horizontal: `${e.prefix}-${s.scrollable}-${B.horizontal}`
  },
  scrollableSelector: `${e.prefix}-${s.scrollable}`,
  scrollablePlaceholder: {
    main: `${e.prefix}-${s.scrollable}-${o.placeholder}`,
    horizontal: `${e.prefix}-${s.scrollable}-${B.horizontal}-${o.placeholder}`
  },
  link: `${e.prefix}-${x.link}`,
  navigationMarker: `${c}-${n.navigation}-${n.marker}`
}, j = {
  wrapper: (t) => {
    const { c: a = u, disabled: r, mobileMode: i } = t, l = a.wrapper;
    return {
      [l.main]: !0,
      [l.infinite]: !0,
      [l.disabled]: r,
      [l.size.large]: i
    };
  },
  view: (t) => {
    const { c: a = u, month: r, year: i, decade: l, century: h } = t, g = a.view;
    return {
      [g.main]: !0,
      [g.vertical]: !0,
      [g.month]: r,
      [g.year]: i,
      [g.decade]: l,
      [g.century]: h
    };
  },
  navigation: d(u, "navigation"),
  navigationHighlight: d(u, "navigationHighlight"),
  table: (t) => {
    const { c: a = u, weekdays: r } = t, i = a.table;
    return {
      [i.main]: !0,
      [i.weekdays]: r
    };
  },
  thead: d(u, "thead"),
  tr: d(u, "tr"),
  th: d(u, "th"),
  tbody: d(u, "tbody"),
  title: d(u, "title"),
  header: (t) => {
    const { c: a = u, vertical: r } = t, i = a.header;
    return {
      [i.main]: !0,
      [i.vertical]: r
    };
  },
  today: (t) => {
    const { c: a = u, disabled: r } = t, i = a.today;
    return {
      [i.main]: !0,
      [i.disabled]: r
    };
  },
  spacer: d(u, "spacer"),
  nav: d(u, "nav"),
  caption: d(u, "caption"),
  ul: d(u, "ul"),
  li: d(u, "li"),
  td: (t) => {
    const {
      c: a = u,
      rangeStart: r,
      rangeEnd: i,
      rangeMid: l,
      rangeSplitEnd: h,
      rangeSplitStart: g,
      active: k,
      focused: $,
      selected: D,
      today: F,
      weekend: T,
      disabled: G,
      isOtherMonth: H,
      isEmpty: C,
      isWeek: E
    } = t, m = a.td;
    return E ? {
      [m.main]: !0,
      [m.isWeek]: E
    } : C ? {
      [m.main]: !0,
      [m.isEmpty]: C
    } : {
      [m.main]: !0,
      [m.rangeStart]: r,
      [m.rangeEnd]: i,
      [m.rangeMid]: l,
      [m.rangeSplitEnd]: h,
      [m.rangeSplitStart]: g,
      [m.active]: k,
      [m.focused]: $,
      [m.selected]: D,
      [m.today]: F,
      [m.weekend]: T,
      [m.disabled]: G,
      [m.isOtherMonth]: H
    };
  },
  scrollable: (t) => {
    const { c: a = u, horizontal: r } = t, i = a.scrollable;
    return {
      [i.main]: !0,
      [i.content]: !0,
      [i.horizontal]: r
    };
  },
  scrollableSelector: d(u, "scrollableSelector"),
  scrollablePlaceholder: (t) => {
    const { c: a = u, horizontal: r } = t, i = a.scrollablePlaceholder;
    return {
      [i.main]: !0,
      [i.horizontal]: r
    };
  },
  link: d(u, "link"),
  navigationMarker: (t) => {
    const { c: a = u, isRangeStart: r } = t;
    return {
      [a.navigationMarker]: r
    };
  }
}, S = {
  wrapper: {
    main: `${e.prefix}-${v.dateinput}`,
    input: `${p}`,
    size: {
      small: `${p}-${b.small}`,
      medium: `${p}-${b.medium}`,
      large: `${p}-${b.large}`
    },
    fillMode: {
      solid: `${p}-${M.solid}`,
      outline: `${p}-${M.outline}`,
      flat: `${p}-${M.flat}`
    },
    rounded: {
      small: `${e.prefix}-${e.rounded}-${z.small}`,
      medium: `${e.prefix}-${e.rounded}-${z.medium}`,
      large: `${e.prefix}-${e.rounded}-${z.large}`
    },
    disabled: `${e.prefix}-${s.disabled}`,
    required: `${e.prefix}-${s.required}`,
    invalid: `${e.prefix}-${s.invalid}`
  },
  inputInner: `${p}-${W.inner}`,
  inputSpinner: {
    main: `${p}-${W.spinner}`,
    button: `${e.prefix}-${W.spin}-${x.button}`
  },
  spinnerIncrease: `${e.prefix}-${W.spinner}-${y.increase}`,
  spinnerDecrease: `${e.prefix}-${W.spinner}-${y.decrease}`,
  clearButton: `${e.prefix}-${e.clear}-${e.value}`
}, A = {
  wrapper: (t) => {
    const { c: a = S, size: r, fillMode: i, rounded: l, disabled: h, required: g, invalid: k } = t, $ = a.wrapper;
    return {
      [$.main]: !0,
      [$.input]: !0,
      [$.size[r]]: $.size[r],
      [`${p}-${r}`]: r && !$.size[r],
      [$.fillMode[i]]: $.fillMode[i],
      [$.rounded[l]]: $.rounded[l],
      [`${e.prefix}-${e.rounded}-${l}`]: l && !$.rounded[l],
      [$.disabled]: h,
      [$.invalid]: k,
      [$.required]: g
    };
  },
  inputInner: d(S, "inputInner"),
  inputSpinner: (t) => {
    const { c: a = S } = t, r = a.inputSpinner;
    return {
      [r.main]: !0,
      [r.button]: !0
    };
  },
  spinnerIncrease: d(S, "spinnerIncrease"),
  spinnerDecrease: d(S, "spinnerDecrease"),
  clearButton: d(S, "clearButton")
}, w = {
  wrapper: {
    main: `${e.prefix}-${v.datetimepicker}`,
    input: `${p}`,
    size: {
      small: `${p}-${b.small}`,
      medium: `${p}-${b.medium}`,
      large: `${p}-${b.large}`
    },
    fillMode: {
      solid: `${p}-${M.solid}`,
      outline: `${p}-${M.outline}`,
      flat: `${p}-${M.flat}`
    },
    rounded: {
      small: `${e.prefix}-${e.rounded}-${z.small}`,
      medium: `${e.prefix}-${e.rounded}-${z.medium}`,
      large: `${e.prefix}-${e.rounded}-${z.large}`
    },
    disabled: `${e.prefix}-${s.disabled}`,
    required: `${e.prefix}-${s.required}`,
    invalid: `${e.prefix}-${s.invalid}`
  },
  inputButton: `${p}-${x.button}`,
  popup: {
    main: `${e.prefix}-${v.datetime}-${o.container}`,
    reset: `${e.prefix}-${e.reset}`
  },
  wrap: {
    main: `${e.prefix}-${v.datetime}-${o.wrap}`,
    date: `${e.prefix}-${n.date}-${o.tab}`,
    time: `${e.prefix}-${n.time}-${o.tab}`,
    disabled: `${e.prefix}-${s.disabled}`
  },
  timeFooter: {
    main: `${e.prefix}-${v.datetime}-${x.footer}`,
    actions: `${e.prefix}-${o.actions}`,
    stretched: `${e.prefix}-${o.actions}-${s.stretched}`
  },
  buttonGroup: `${e.prefix}-${v.datetime}-${v.buttongroup}`,
  selector: `${e.prefix}-${v.datetime}-${n.selector}`,
  calendarWrap: `${e.prefix}-${v.datetime}-${c}-${o.wrap}`,
  timeWrap: `${e.prefix}-${v.datetime}-${n.time}-${o.wrap}`,
  timeSelector: {
    main: `${e.prefix}-${n.timeselector}`,
    size: {
      medium: `${e.prefix}-${n.timeselector}-${b.medium}`,
      large: `${e.prefix}-${n.timeselector}-${b.large}`
    },
    disabled: `${e.prefix}-${s.disabled}`,
    reset: `${e.prefix}-${e.reset}`
  }
}, J = {
  wrapper: (t) => {
    const { c: a = w, size: r, fillMode: i, rounded: l, disabled: h, required: g, invalid: k } = t, $ = a.wrapper;
    return {
      [$.main]: !0,
      [$.input]: !0,
      [$.size[r]]: $.size[r],
      [`${p}-${r}`]: r && !$.size[r],
      [$.fillMode[i]]: $.fillMode[i],
      [$.rounded[l]]: $.rounded[l],
      [`${e.prefix}-${e.rounded}-${l}`]: l && !$.rounded[l],
      [$.disabled]: h,
      [$.invalid]: k,
      [$.required]: g
    };
  },
  inputButton: d(w, "inputButton"),
  popup: (t) => {
    const { c: a = w } = t, r = a.popup;
    return {
      [r.main]: !0,
      [r.reset]: !0
    };
  },
  wrap: (t) => {
    const { c: a = w, disabled: r, date: i, time: l } = t, h = a.wrap;
    return {
      [h.main]: !0,
      [h.time]: l,
      [h.date]: i,
      [h.disabled]: r
    };
  },
  timeFooter: (t) => {
    const { c: a = w } = t, r = a.timeFooter;
    return {
      [r.main]: !0,
      [r.actions]: !0,
      [r.stretched]: !0
    };
  },
  buttonGroup: d(w, "buttonGroup"),
  selector: d(w, "selector"),
  calendarWrap: d(w, "calendarWrap"),
  timeWrap: d(w, "timeWrap"),
  timeSelector: (t) => {
    const { c: a = w, mobileMode: r } = t, i = a.timeSelector;
    return {
      [i.main]: r,
      [i.size.large]: r,
      [i.reset]: r
    };
  }
};
export {
  j as uCalendar,
  A as uDateInput,
  J as uDateTimePicker,
  R as uTime,
  U as uTimePicker
};
