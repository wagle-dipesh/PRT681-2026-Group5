/**
 * @license
 *-------------------------------------------------------------------------------------------
 * Copyright © 2026 Progress Software Corporation. All rights reserved.
 * Licensed under commercial license. See LICENSE.md in the package root for more information
 *-------------------------------------------------------------------------------------------
 */
"use client";
import * as t from "react";
const s = t.createContext(null);
s.displayName = "WebMcpContext";
const d = ({ children: r, dataName: o }) => {
  const u = t.useMemo(() => ({ dataName: o }), [o]);
  return /* @__PURE__ */ t.createElement(s.Provider, { value: u }, r);
};
function m() {
  return typeof document != "undefined" && document.modelContext ? document.modelContext : null;
}
function g(r, o, u, e) {
  const n = t.useContext(s), c = t.useRef(u);
  c.current = u;
  const i = t.useMemo(
    () => typeof e == "object" ? JSON.stringify(e) : String(!!e),
    // webMcp is intentionally listed as the only dep — we want a stable key
    // based on the serialised value, not the object reference.
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [e]
  );
  t.useEffect(() => {
    if (!e || !(n != null && n.register) || !o)
      return;
    const f = typeof e == "object" ? e : void 0;
    return n.register(r, o.current, c, f);
  }, [i, n, r, o]);
}
export {
  s as WebMcpContext,
  d as WebMcpProvider,
  m as getModelContext,
  g as useWebMcpRegister
};
