/**
 * @license
 *-------------------------------------------------------------------------------------------
 * Copyright © 2026 Progress Software Corporation. All rights reserved.
 * Licensed under commercial license. See LICENSE.md in the package root for more information
 *-------------------------------------------------------------------------------------------
 */
"use client";
import * as d from "react";
import { HeaderThElement as c } from "./HeaderThElement.mjs";
import { getFilterColumnId as C } from "../navigation/utils.mjs";
import { useLocalization as k } from "@progress/kendo-react-intl";
import { filterAriaLabel as f, messages as p } from "../messages/index.mjs";
import { classNames as b } from "@progress/kendo-react-common";
const w = "k-table-row k-filter-row", F = (a, i, l) => {
  let r = `${l ? "k-grid-header-sticky" : ""}`;
  return a.some((o) => o.field === i) && (r += " k-sorted"), r;
}, E = function(a) {
  const { columns: i, filter: l, filterChange: r, sort: o = [], ariaRowIndex: m } = a, g = k(), h = (e) => {
    if (!r)
      return;
    const n = e.filter.filter((t) => !(t.operator === "eq" && t.value === null));
    r({ ...e, filter: n });
  };
  return /* @__PURE__ */ d.createElement("tr", { className: w, "aria-rowindex": m, role: "row" }, i.map((e, n) => {
    const t = e.locked !== void 0 ? {
      left: e.left,
      right: e.right,
      borderRightWidth: e.rightBorder ? "1px" : ""
    } : {}, s = e.filterCell || e.filter;
    return /* @__PURE__ */ d.createElement(
      c,
      {
        columnId: C(e.id),
        navigatable: e.navigatable,
        role: "columnheader",
        style: t,
        key: n,
        ariaColumnIndex: e.ariaColumnIndex,
        ariaLabel: s ? g.toLanguageString(f, p[f]) : void 0,
        className: b(
          "k-table-th k-header",
          F(o, e.field, e.locked)
        )
      },
      s && /* @__PURE__ */ d.createElement(s, { field: e.field, filter: l, onFilterChange: h })
    );
  }));
};
export {
  w as FILTER_ROW_CLASS,
  E as FilterRow
};
