/**
 * @license
 *-------------------------------------------------------------------------------------------
 * Copyright © 2026 Progress Software Corporation. All rights reserved.
 * Licensed under commercial license. See LICENSE.md in the package root for more information
 *-------------------------------------------------------------------------------------------
 */
import { UploadFileInfo } from './interfaces/UploadFileInfo';
import { UploadProps } from './interfaces/UploadProps';
import * as React from 'react';
/**
 * Represent the `ref` of the Upload component.
 */
export interface UploadHandle {
    /** The current root DOM element or `null`. */
    element: HTMLInputElement | null | undefined;
    /** The props of the Upload component. */
    readonly props: Readonly<UploadProps>;
    /** Focuses the Upload component. */
    focus: () => void;
    /** The action element (hidden file input) of the Upload. */
    actionElement: HTMLInputElement | null | undefined;
    /** Upload the selected files. Usable when the upload action is triggered by external code. */
    triggerUpload: () => void;
    /** Uploads the provided grouped files. */
    uploadFiles: (files: {
        [uid: string]: UploadFileInfo[];
    }) => void;
    /** @hidden */
    onAdd: (files: FileList) => void;
}
/** The default props of the Upload component. */
export declare const uploadDefaultProps: Partial<UploadProps>;
export declare const Upload: React.ForwardRefExoticComponent<UploadProps & React.RefAttributes<UploadHandle>>;
